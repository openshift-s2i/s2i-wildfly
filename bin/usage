#!/bin/bash
echo "This is a Source To Image (STI) builder image.
To use it, install STI: https://github.com/openshift/docker-source-to-images. 

Sample invocation: 
    sti build git://github.com/bparees/openshift-jee-sample openshift/centos-wildfly-standalone wildflytest -s https://raw.githubusercontent.com/openshift/master/wildfly-8-centos/.sti_scripts
    
You can then run the resulting image via:
    docker run -p :8080 wildflytest    
"
